import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drawve.app',
  appName: 'Rifas el mocho',
  webDir: '../drawveBackend/public'
};

export default config;
